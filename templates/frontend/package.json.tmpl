{
  "name": "{{PROJECT_NAME}}-frontend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "echo 'Run: npm create vite@latest . -- --template react-ts' && exit 1",
    "build": "mkdir -p dist && echo '<!DOCTYPE html><html><body><h1>Set up your frontend framework</h1></body></html>' > dist/index.html",
    "lint": "echo 'No lint configured'"
  }
}
